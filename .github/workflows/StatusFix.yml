name: Fix metricReport

on:
  workflow_dispatch:
    inputs:
      test_type:
        description: 'Select which performance test to run'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - smoke
          - load
          - spike

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    # ----------------------------------------------------
    # Setup k6 NATIVELY (NO DOCKER)
    # ----------------------------------------------------
    - name: Setup k6
      uses: grafana/setup-k6-action@v1

    # ----------------------------------------------------
    # Decide which tests to run
    # ----------------------------------------------------
    - name: Determine which tests to run
      run: |
        TEST_TYPE="${{ github.event.inputs.test_type }}"

        if [ "$TEST_TYPE" = "smoke" ]; then
          echo "RUN_SMOKE=true" >> $GITHUB_ENV
          echo "RUN_LOAD=false" >> $GITHUB_ENV
          echo "RUN_SPIKE=false" >> $GITHUB_ENV
        elif [ "$TEST_TYPE" = "load" ]; then
          echo "RUN_SMOKE=false" >> $GITHUB_ENV
          echo "RUN_LOAD=true" >> $GITHUB_ENV
          echo "RUN_SPIKE=false" >> $GITHUB_ENV
        elif [ "$TEST_TYPE" = "spike" ]; then
          echo "RUN_SMOKE=false" >> $GITHUB_ENV
          echo "RUN_LOAD=false" >> $GITHUB_ENV
          echo "RUN_SPIKE=true" >> $GITHUB_ENV
        else
          echo "RUN_SMOKE=true" >> $GITHUB_ENV
          echo "RUN_LOAD=true" >> $GITHUB_ENV
          echo "RUN_SPIKE=true" >> $GITHUB_ENV
        fi

    # ----------------------------------------------------
    # Run selected tests with NATIVE k6 (NO DOCKER)
    # ----------------------------------------------------
    - name: Run Selected Performance Tests
      run: |
        declare -A TESTS=(
          ["smoke"]="tests/performance/smoke/dummyjson-smoke.js"
          ["load"]="tests/performance/load/dummyjson-load.js"
          ["spike"]="tests/performance/spike/dummyjson-spike.js"
        )

        for TEST in smoke load spike; do
          RUN_VAR="RUN_${TEST^^}"
          if [ "${!RUN_VAR}" = "true" ]; then
            echo "🚀 Running ${TEST^} test..."
            echo "Test file: ${TESTS[$TEST]}"
            
            # Check if file exists
            if [ ! -f "${TESTS[$TEST]}" ]; then
              echo "❌ Error: File ${TESTS[$TEST]} not found!"
              echo "Available files in tests/:"
              find tests -name "*.js" -type f | head -20
              continue
            fi
            
            # Run with JSON output for better metrics
            k6 run "${TESTS[$TEST]}" \
              --out json="k6-${TEST}-results.json" \
              2>&1 | tee "k6-${TEST}-output.txt"
            
            # Check exit code
            if [ $? -eq 0 ]; then
              echo "✅ ${TEST^} test completed successfully"
            else
              echo "❌ ${TEST^} test failed"
            fi
          fi
        done

    # ----------------------------------------------------
    # Report: GRAPH + METRICS PER TEST (ENHANCED)
    # ----------------------------------------------------
    - name: Create Performance Report
      run: |
        echo "## 🧪 K6 Performance Test Report" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "*Report generated at: $(date)*" >> $GITHUB_STEP_SUMMARY
        echo "*Selected test type: ${{ github.event.inputs.test_type }}*" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        render_test () {
          NAME=$1
          TITLE=$2
          MAX_VUS=$3
          LINE=$4
          OUTPUT_FILE="k6-${NAME}-output.txt"

          if [ -f "$OUTPUT_FILE" ]; then
            echo "### 🚦 ${TITLE} Test" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            # -------- TEST STATUS --------
            STATUS="COMPLETED"
            
            if grep -q "thresholds on metrics.*have been crossed" "$OUTPUT_FILE"; then
              STATUS="THRESHOLD_VIOLATIONS"
            fi
            
            if grep -qi "ERRO\|ERROR\|Error:" "$OUTPUT_FILE"; then
              if ! grep -q "thresholds on metrics.*have been crossed" "$OUTPUT_FILE"; then
                STATUS="ERROR"
              fi
            fi
            
            if ! grep -q "iterations.*:" "$OUTPUT_FILE" && ! grep -q "http_reqs.*:" "$OUTPUT_FILE"; then
              STATUS="FAILED_NO_OUTPUT"
            fi
            
            case "$STATUS" in
              "COMPLETED")
                echo "✅ **Status: COMPLETED SUCCESSFULLY**" >> $GITHUB_STEP_SUMMARY
                ;;
              "THRESHOLD_VIOLATIONS")
                echo "⚠️ **Status: COMPLETED WITH THRESHOLD VIOLATIONS**" >> $GITHUB_STEP_SUMMARY
                ;;
              "ERROR")
                echo "❌ **Status: FAILED WITH ERRORS**" >> $GITHUB_STEP_SUMMARY
                ;;
              "FAILED_NO_OUTPUT")
                echo "❌ **Status: FAILED - NO METRICS GENERATED**" >> $GITHUB_STEP_SUMMARY
                ;;
              *)
                echo "✅ **Status: COMPLETED**" >> $GITHUB_STEP_SUMMARY
                ;;
            esac
            echo "" >> $GITHUB_STEP_SUMMARY

            # -------- GRAPH --------
            echo "#### 📈 Load Pattern" >> $GITHUB_STEP_SUMMARY
            echo '```mermaid' >> $GITHUB_STEP_SUMMARY
            echo 'xychart-beta' >> $GITHUB_STEP_SUMMARY
            echo "    title \"${TITLE} Test Load Pattern\"" >> $GITHUB_STEP_SUMMARY
            echo '    x-axis "Time (minutes)"' >> $GITHUB_STEP_SUMMARY
            echo "    y-axis \"Virtual Users\" 0 --> ${MAX_VUS}" >> $GITHUB_STEP_SUMMARY
            echo "    line ${LINE}" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            # -------- SIMPLE LANGUAGE SECTION FOR MANUAL TESTERS --------
            echo "#### 🧑‍💻 Plain English Explanation" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**What this test means for the business:**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # Extract key metrics for simple explanation
            FAILED_RATE=$(grep "http_req_failed.*rate=" "$OUTPUT_FILE" | tail -1 | sed 's/.*rate=//' | awk '{print $1}' 2>/dev/null || echo "0")
            P95_RESPONSE=$(grep "http_req_duration.*p(95)=" "$OUTPUT_FILE" | tail -1 | sed 's/.*p(95)=//' | awk '{print $1}' 2>/dev/null || echo "N/A")
            P99_RESPONSE=$(grep "http_req_duration.*p(99)=" "$OUTPUT_FILE" | tail -1 | sed 's/.*p(99)=//' | awk '{print $1}' 2>/dev/null || echo "N/A")
            CHECKS_TOTAL=$(grep "checks_total.*:" "$OUTPUT_FILE" | tail -1 | awk '{print $2}' | tr -d ',' 2>/dev/null || echo "0")
            CHECKS_FAILED=$(grep "checks_failed.*:" "$OUTPUT_FILE" | tail -1 | awk '{print $2}' | tr -d ',' 2>/dev/null || echo "0")
            
            # Calculate percentages
            if [ "$FAILED_RATE" != "0" ] && [ "$FAILED_RATE" != "N/A" ]; then
              FAILED_PERCENT=$(echo "$FAILED_RATE * 100" | bc -l 2>/dev/null | xargs printf "%.1f" || echo "$FAILED_RATE")
              echo "🔴 **Failed Requests:** ${FAILED_PERCENT}% of requests had errors" >> $GITHUB_STEP_SUMMARY
            else
              echo "🟢 **Failed Requests:** Very low error rate" >> $GITHUB_STEP_SUMMARY
            fi
            
            if [ "$P95_RESPONSE" != "N/A" ]; then
              echo "⚡ **Response Speed (95% of requests):** ${P95_RESPONSE}ms" >> $GITHUB_STEP_SUMMARY
            fi
            
            if [ "$CHECKS_TOTAL" != "0" ] && [ "$CHECKS_TOTAL" != "N/A" ]; then
              if [ "$CHECKS_FAILED" != "N/A" ]; then
                if [ "$CHECKS_TOTAL" -gt 0 ]; then
                  CHECKS_PASSED=$((CHECKS_TOTAL - CHECKS_FAILED))
                  CHECKS_SUCCESS_RATE=$((CHECKS_PASSED * 100 / CHECKS_TOTAL))
                  if [ "$CHECKS_SUCCESS_RATE" -lt 90 ]; then
                    echo "🔴 **Functionality Checks:** Only ${CHECKS_SUCCESS_RATE}% passed" >> $GITHUB_STEP_SUMMARY
                  else
                    echo "🟢 **Functionality Checks:** ${CHECKS_SUCCESS_RATE}% passed" >> $GITHUB_STEP_SUMMARY
                  fi
                fi
              fi
            fi
            
            # Analyze threshold results for simple explanation
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Threshold Analysis:**" >> $GITHUB_STEP_SUMMARY
            
            # Look for specific threshold patterns in the output
            if grep -q "❌ 'rate>0.85'" "$OUTPUT_FILE"; then
              RATE=$(grep "❌ 'rate>0.85'" "$OUTPUT_FILE" | sed "s/.*rate=//" | sed "s/%//")
              USER_RATE=$(echo "$RATE" | sed 's/[^0-9.]//g')
              if [ -n "$USER_RATE" ]; then
                echo "🔴 **User Experience:** Only ${USER_RATE}% of users had good experience (needs 85%+)" >> $GITHUB_STEP_SUMMARY
              fi
            fi
            
            if grep -q "❌ 'rate<0.10'" "$OUTPUT_FILE"; then
              RATE=$(grep "❌ 'rate<0.10'" "$OUTPUT_FILE" | sed "s/.*rate=//" | sed "s/%//")
              ERROR_RATE=$(echo "$RATE" | sed 's/[^0-9.]//g')
              if [ -n "$ERROR_RATE" ]; then
                echo "🔴 **System Errors:** ${ERROR_RATE}% error rate (should be under 10%)" >> $GITHUB_STEP_SUMMARY
              fi
            fi
            
            if grep -q "✅ 'p(95)<2000'" "$OUTPUT_FILE"; then
              P95=$(grep "✅ 'p(95)<2000'" "$OUTPUT_FILE" | sed "s/.*p(95)=//" | sed "s/ms//")
              echo "🟢 **Speed Test:** 95% of requests faster than ${P95}ms (under 2000ms target)" >> $GITHUB_STEP_SUMMARY
            fi
            
            # Check for page load metrics
            if grep -q "page loaded during spike" "$OUTPUT_FILE"; then
              PAGE_LOAD=$(grep -A1 "page loaded during spike" "$OUTPUT_FILE" | tail -1 | grep -o "[0-9]*%" | head -1)
              if [ -n "$PAGE_LOAD" ]; then
                echo "🔍 **Page Load Success:** ${PAGE_LOAD} of pages loaded during spike" >> $GITHUB_STEP_SUMMARY
              fi
            fi
            
            echo "" >> $GITHUB_STEP_SUMMARY

            # -------- ENHANCED METRICS DISPLAY --------
            echo "#### 📊 Performance Metrics" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # Extract key metrics
            ITERATIONS=$(grep "iterations.*:" "$OUTPUT_FILE" | tail -1 | awk '{print $2}' | tr -d ',' 2>/dev/null || echo "N/A")
            HTTP_REQS=$(grep "http_reqs.*:" "$OUTPUT_FILE" | tail -1 | awk '{print $2}' | tr -d ',' 2>/dev/null || echo "N/A")
            AVG_RESPONSE=$(grep "http_req_duration.*avg=" "$OUTPUT_FILE" | tail -1 | sed 's/.*avg=//' | awk '{print $1}' 2>/dev/null || echo "N/A")
            FAILED_RATE=$(grep "http_req_failed.*rate=" "$OUTPUT_FILE" | tail -1 | sed 's/.*rate=//' | awk '{print $1}' 2>/dev/null || echo "N/A")
            DATA_RECEIVED=$(grep "data_received.*:" "$OUTPUT_FILE" | tail -1 | sed 's/.*://' | awk '{$1=$1};1' 2>/dev/null || echo "N/A")
            DATA_SENT=$(grep "data_sent.*:" "$OUTPUT_FILE" | tail -1 | sed 's/.*://' | awk '{$1=$1};1' 2>/dev/null || echo "N/A")
            
            # Extract percentile metrics
            MIN_RESPONSE=$(grep "http_req_duration.*min=" "$OUTPUT_FILE" | tail -1 | sed 's/.*min=//' | awk '{print $1}' 2>/dev/null || echo "N/A")
            MED_RESPONSE=$(grep "http_req_duration.*med=" "$OUTPUT_FILE" | tail -1 | sed 's/.*med=//' | awk '{print $1}' 2>/dev/null || echo "N/A")
            MAX_RESPONSE=$(grep "http_req_duration.*max=" "$OUTPUT_FILE" | tail -1 | sed 's/.*max=//' | awk '{print $1}' 2>/dev/null || echo "N/A")
            P90_RESPONSE=$(grep "http_req_duration.*p(90)=" "$OUTPUT_FILE" | tail -1 | sed 's/.*p(90)=//' | awk '{print $1}' 2>/dev/null || echo "N/A")
            P95_RESPONSE=$(grep "http_req_duration.*p(95)=" "$OUTPUT_FILE" | tail -1 | sed 's/.*p(95)=//' | awk '{print $1}' 2>/dev/null || echo "N/A")
            P99_RESPONSE=$(grep "http_req_duration.*p(99)=" "$OUTPUT_FILE" | tail -1 | sed 's/.*p(99)=//' | awk '{print $1}' 2>/dev/null || echo "N/A")
            
            # Extract check success rate
            CHECKS_TOTAL=$(grep "checks_total.*:" "$OUTPUT_FILE" | tail -1 | awk '{print $2}' | tr -d ',' 2>/dev/null || echo "N/A")
            CHECKS_SUCCEEDED=$(grep "checks_succeeded.*:" "$OUTPUT_FILE" | tail -1 | awk '{print $2}' | tr -d ',' 2>/dev/null || echo "N/A")
            CHECKS_FAILED=$(grep "checks_failed.*:" "$OUTPUT_FILE" | tail -1 | awk '{print $2}' | tr -d ',' 2>/dev/null || echo "N/A")
            
            # Extract VU metrics
            VUS_MIN=$(grep "vus.*min=" "$OUTPUT_FILE" | tail -1 | sed 's/.*min=//' | awk '{print $1}' 2>/dev/null || echo "N/A")
            VUS_MAX=$(grep "vus.*max=" "$OUTPUT_FILE" | tail -1 | sed 's/.*max=//' | awk '{print $1}' 2>/dev/null || echo "N/A")
            
            # 🎯 THRESHOLDS SECTION WITH SIMPLE INTERPRETATION
            echo "##### 🎯 Thresholds Status" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            # Capture threshold lines and add simple comments
            THRESHOLD_LINES=$(grep -A1 "✓\|✗" "$OUTPUT_FILE" | head -20)
            echo "$THRESHOLD_LINES" | while IFS= read -r LINE; do
              if echo "$LINE" | grep -q "✗ 'rate>0.85'"; then
                RATE=$(echo "$LINE" | grep -o "rate=[0-9.]*%" | cut -d= -f2)
                echo "$LINE   # 🔴 Only ${RATE} users had good experience (need 85%+)" >> $GITHUB_STEP_SUMMARY
              elif echo "$LINE" | grep -q "✗ 'rate<0.10'"; then
                RATE=$(echo "$LINE" | grep -o "rate=[0-9.]*%" | cut -d= -f2)
                echo "$LINE   # 🔴 ${RATE} error rate - system failing!" >> $GITHUB_STEP_SUMMARY
              elif echo "$LINE" | grep -q "✓ 'p(95)<2000'"; then
                VALUE=$(echo "$LINE" | grep -o "p(95)=[0-9.]*" | cut -d= -f2)
                echo "$LINE   # 🟢 Fast! 95% under ${VALUE}ms" >> $GITHUB_STEP_SUMMARY
              elif echo "$LINE" | grep -q "✗ page loaded during spike"; then
                echo "$LINE   # 🔴 Page load failing during traffic spike" >> $GITHUB_STEP_SUMMARY
              elif echo "$LINE" | grep -q "✗ popular product loaded"; then
                echo "$LINE   # 🔴 Can't view products during busy times" >> $GITHUB_STEP_SUMMARY
              else
                echo "$LINE" >> $GITHUB_STEP_SUMMARY
              fi
            done 2>/dev/null
            
            if [ -z "$THRESHOLD_LINES" ]; then
              echo "No threshold data found" >> $GITHUB_STEP_SUMMARY
            fi
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # 📈 RESPONSE TIME METRICS TABLE
            echo "##### ⚡ Response Time Distribution" >> $GITHUB_STEP_SUMMARY
            echo '| Metric | Value | What It Means |' >> $GITHUB_STEP_SUMMARY
            echo '|--------|-------|---------------|' >> $GITHUB_STEP_SUMMARY
            if [ "$MIN_RESPONSE" != "N/A" ]; then echo "| Fastest | $MIN_RESPONSE ms | Best case scenario |" >> $GITHUB_STEP_SUMMARY; fi
            if [ "$AVG_RESPONSE" != "N/A" ]; then echo "| Average | $AVG_RESPONSE ms | Typical response time |" >> $GITHUB_STEP_SUMMARY; fi
            if [ "$MED_RESPONSE" != "N/A" ]; then echo "| Median | $MED_RESPONSE ms | Middle response time |" >> $GITHUB_STEP_SUMMARY; fi
            if [ "$P90_RESPONSE" != "N/A" ]; then echo "| 90th %ile | $P90_RESPONSE ms | 90% faster than this |" >> $GITHUB_STEP_SUMMARY; fi
            if [ "$P95_RESPONSE" != "N/A" ]; then echo "| 95th %ile | $P95_RESPONSE ms | 95% faster than this |" >> $GITHUB_STEP_SUMMARY; fi
            if [ "$P99_RESPONSE" != "N/A" ]; then echo "| 99th %ile | $P99_RESPONSE ms | 99% faster than this |" >> $GITHUB_STEP_SUMMARY; fi
            if [ "$MAX_RESPONSE" != "N/A" ]; then echo "| Slowest | $MAX_RESPONSE ms | Worst case scenario |" >> $GITHUB_STEP_SUMMARY; fi
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # 📊 VOLUME METRICS TABLE
            echo "##### 📊 Volume Metrics" >> $GITHUB_STEP_SUMMARY
            echo '| Metric | Value |' >> $GITHUB_STEP_SUMMARY
            echo '|--------|-------|' >> $GITHUB_STEP_SUMMARY
            if [ "$ITERATIONS" != "N/A" ]; then echo "| Iterations | $ITERATIONS |" >> $GITHUB_STEP_SUMMARY; fi
            if [ "$HTTP_REQS" != "N/A" ]; then echo "| HTTP Requests | $HTTP_REQS |" >> $GITHUB_STEP_SUMMARY; fi
            if [ "$VUS_MIN" != "N/A" ] && [ "$VUS_MAX" != "N/A" ]; then echo "| Virtual Users | $VUS_MIN - $VUS_MAX |" >> $GITHUB_STEP_SUMMARY; fi
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # ✅ SUCCESS METRICS TABLE
            echo "##### ✅ Success Metrics" >> $GITHUB_STEP_SUMMARY
            echo '| Metric | Value | Status |' >> $GITHUB_STEP_SUMMARY
            echo '|--------|-------|--------|' >> $GITHUB_STEP_SUMMARY
            if [ "$FAILED_RATE" != "N/A" ]; then 
              FAILED_PERCENT=$(echo "$FAILED_RATE * 100" | bc -l 2>/dev/null | xargs printf "%.2f%%" || echo "$FAILED_RATE")
              if echo "$FAILED_PERCENT" | grep -q "[0-9]"; then
                NUM=$(echo "$FAILED_PERCENT" | sed 's/%//')
                if (( $(echo "$NUM > 10" | bc -l 2>/dev/null) )); then
                  STATUS="🔴 Critical"
                elif (( $(echo "$NUM > 5" | bc -l 2>/dev/null) )); then
                  STATUS="🟡 Warning"
                else
                  STATUS="🟢 Good"
                fi
                echo "| Failure Rate | $FAILED_PERCENT | $STATUS |" >> $GITHUB_STEP_SUMMARY
              fi
            fi
            if [ "$CHECKS_TOTAL" != "N/A" ] && [ "$CHECKS_FAILED" != "N/A" ]; then
              if [ "$CHECKS_TOTAL" -gt 0 ]; then
                SUCCESS_PERCENT=$(( (CHECKS_TOTAL - CHECKS_FAILED) * 100 / CHECKS_TOTAL ))
                if [ "$SUCCESS_PERCENT" -lt 90 ]; then
                  STATUS="🔴 Critical"
                elif [ "$SUCCESS_PERCENT" -lt 95 ]; then
                  STATUS="🟡 Warning"
                else
                  STATUS="🟢 Good"
                fi
                echo "| Check Success Rate | ${SUCCESS_PERCENT}% | $STATUS |" >> $GITHUB_STEP_SUMMARY
              fi
            fi
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # 📡 NETWORK METRICS TABLE
            echo "##### 📡 Network Metrics" >> $GITHUB_STEP_SUMMARY
            echo '| Metric | Value |' >> $GITHUB_STEP_SUMMARY
            echo '|--------|-------|' >> $GITHUB_STEP_SUMMARY
            if [ "$DATA_RECEIVED" != "N/A" ]; then echo "| Data Received | $DATA_RECEIVED |" >> $GITHUB_STEP_SUMMARY; fi
            if [ "$DATA_SENT" != "N/A" ]; then echo "| Data Sent | $DATA_SENT |" >> $GITHUB_STEP_SUMMARY; fi
            echo "" >> $GITHUB_STEP_SUMMARY

            # -------- DETAILED OUTPUT --------
            echo "#### 📄 Detailed Test Output" >> $GITHUB_STEP_SUMMARY
            echo "<details>" >> $GITHUB_STEP_SUMMARY
            echo "<summary>Click to view complete test output</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            # Filter out progress bars and show only important sections
            grep -v "default.*\[.*\].*VUs.*s/" "$OUTPUT_FILE" | \
              grep -v "running.*s),.*VUs.*complete" | \
              tail -100 >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "---" >> $GITHUB_STEP_SUMMARY
          else
            echo "### 🚦 ${TITLE} Test" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "❌ **Test not run or output file missing**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "---" >> $GITHUB_STEP_SUMMARY
          fi
        }

        # Render each test that was run
        [ "${RUN_SMOKE}" = "true" ] && render_test "smoke" "Smoke" 5 "[1, 3, 5, 0]"
        [ "${RUN_LOAD}" = "true" ] && render_test "load" "Load" 120 "[10, 30, 80, 120, 60, 20]"
        [ "${RUN_SPIKE}" = "true" ] && render_test "spike" "Spike" 400 "[5, 50, 200, 400, 200, 50, 10]"

        # -------- EXECUTIVE SUMMARY WITH SIMPLE LANGUAGE --------
        echo "### 📝 Executive Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Tests Executed:** ${{ github.event.inputs.test_type }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        # Determine overall test status
        OVERALL_STATUS="🟢 PASS"
        if [ -f "k6-spike-output.txt" ] && grep -q "✗" "k6-spike-output.txt"; then
          if grep -q "✗ 'rate<0.10'" "k6-spike-output.txt"; then
            RATE=$(grep "✗ 'rate<0.10'" "k6-spike-output.txt" | sed "s/.*rate=//" | sed "s/%//")
            OVERALL_STATUS="🔴 CRITICAL FAILURE"
            echo "### 🚨 CRITICAL ISSUE DETECTED" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Problem:** System has **${RATE} error rate** during traffic spikes" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**What this means:**" >> $GITHUB_STEP_SUMMARY
            echo "- During busy times, most users will get errors" >> $GITHUB_STEP_SUMMARY
            echo "- Website becomes mostly unusable under load" >> $GITHUB_STEP_SUMMARY
            echo "- Urgent fixes needed before any promotions/events" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          elif grep -q "✗ 'rate>0.85'" "k6-spike-output.txt"; then
            RATE=$(grep "✗ 'rate>0.85'" "k6-spike-output.txt" | sed "s/.*rate=//" | sed "s/%//")
            OVERALL_STATUS="🟡 WARNING"
            echo "### ⚠️ WARNING: User Experience Issues" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Problem:** Only **${RATE} of users** had good experience during spike" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi
        fi
        
        echo "**Overall Status:** $OVERALL_STATUS" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        echo "**Key Insights:**" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        # Add insights based on actual test results
        if [ -f "k6-spike-output.txt" ]; then
          if grep -q "✅ 'p(95)<2000'" "k6-spike-output.txt"; then
            P95=$(grep "✅ 'p(95)<2000'" "k6-spike-output.txt" | sed "s/.*p(95)=//" | sed "s/ms//")
            echo "- 🟢 **Speed is Excellent:** 95% of responses under ${P95}ms" >> $GITHUB_STEP_SUMMARY
          fi
          
          if grep -q "✗ page loaded during spike" "k6-spike-output.txt"; then
            echo "- 🔴 **Page Load Failing:** Most pages won't load during traffic spikes" >> $GITHUB_STEP_SUMMARY
          fi
          
          if grep -q "✗ popular product loaded" "k6-spike-output.txt"; then
            echo "- 🔴 **Product Pages Broken:** Users can't view popular items when busy" >> $GITHUB_STEP_SUMMARY
          fi
        fi
        
        if [ -f "k6-load-output.txt" ] && grep -q "✅" "k6-load-output.txt"; then
          echo "- 🟢 **Normal Load OK:** System handles regular traffic well" >> $GITHUB_STEP_SUMMARY
        fi
        
        if [ -f "k6-smoke-output.txt" ] && grep -q "✅" "k6-smoke-output.txt"; then
          echo "- 🟢 **Basic Functionality OK:** Core features work correctly" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Recommendations:**" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        # Add targeted recommendations
        if [ "$OVERALL_STATUS" = "🔴 CRITICAL FAILURE" ]; then
          echo "1. **🚨 IMMEDIATE ACTION:** Fix the high error rate during spikes" >> $GITHUB_STEP_SUMMARY
          echo "2. **Investigate:** Why pages fail to load under 400 users" >> $GITHUB_STEP_SUMMARY
          echo "3. **Test Again:** Don't deploy until spike tests pass" >> $GITHUB_STEP_SUMMARY
        elif [ "$OVERALL_STATUS" = "🟡 WARNING" ]; then
          echo "1. **Address Soon:** Improve user experience during busy times" >> $GITHUB_STEP_SUMMARY
          echo "2. **Monitor:** Watch response time percentiles in production" >> $GITHUB_STEP_SUMMARY
          echo "3. **Plan Scaling:** Consider scaling if user load exceeds tested patterns" >> $GITHUB_STEP_SUMMARY
        else
          echo "1. **Monitor:** Response time percentiles (p95, p99) in production" >> $GITHUB_STEP_SUMMARY
          echo "2. **Scale Planning:** Consider scaling if user load exceeds tested patterns" >> $GITHUB_STEP_SUMMARY
          echo "3. **Regular Testing:** Continue performance testing regularly" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**What to tell stakeholders:**" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [ "$OVERALL_STATUS" = "🔴 CRITICAL FAILURE" ]; then
          echo "> 'The website crashes during busy periods. We need to fix this urgently before running any promotions or events, otherwise most customers will get errors.'" >> $GITHUB_STEP_SUMMARY
        elif [ "$OVERALL_STATUS" = "🟡 WARNING" ]; then
          echo "> 'The website works but many users have a poor experience during peak times. We should improve this before it affects customer satisfaction.'" >> $GITHUB_STEP_SUMMARY
        else
          echo "> 'Performance is good under normal conditions. The site is fast and stable for typical usage patterns.'" >> $GITHUB_STEP_SUMMARY
        fi
